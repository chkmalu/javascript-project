pipeline {
    agent any

    stages {
        stage('Test app') {
            steps {
                echo 'Testing application'
            }
        }
        stage('Build image') {
            steps {
                echo 'Building image'
            }
        }
        stage('Deploy image') {
            steps {
                echo 'Deploying image on prod'
            }
        }
    }
}
